import { SlideShell } from '../components/ui'

/* Placeholder — this becomes the "design and label your avatar" task
 * (Avataaars builder + worked example + success criteria). Rebuilding next. */
export function AvatarBuilderSlide() {
  return (
    <SlideShell kicker="Task" title="Design and label your avatar" intro="Being rebuilt with the Avataaars builder.">
      <p style={{ color: 'var(--color-ink-faint)' }}>Coming next.</p>
    </SlideShell>
  )
}
